local api = "https://api.ipify.org/?format=json"
local HttpService = game:GetService("HttpService")
local request = http_request or request
local abc = tostring(game:GetService("RbxAnalyticsService"):GetClientId())
_G.ZGWRSCRSSGTT = false

function GetApi()
    local a = game:HttpGet(api)
    local b = HttpService:JSONDecode(a)
    local c = b.ip
    if c ~= nil then
        return c
    end
end

_G.KTJFUWIDF = {
    "15.235.164.232"
}

for _, v2 in pairs(_G.KTJFUWIDF) do
    local v = GetApi()
    if tostring(v) == v2 then
        _G.ZGWRSCRSSGTT = true
        break
    end
end
